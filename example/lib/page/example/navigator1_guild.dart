import 'package:flutter/material.dart';

class Navigator1ExamplePage extends StatelessWidget {
  const Navigator1ExamplePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('navigator1的示例'),
      ),
    );
  }
}
